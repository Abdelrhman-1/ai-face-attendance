# dlib must be installed separately on Windows using a prebuilt wheel,
# since it requires a C++ compiler to build from source. See README.
# dlib==20.0.99

face_recognition==1.3.0
face-recognition-models==0.3.0
opencv-python-headless==5.0.0.93
numpy==2.5.1
Flask==3.1.3
supabase==2.31.0
python-dotenv==1.2.2

# Pinned below 81 to avoid removal of pkg_resources, which
# face-recognition-models still depends on.
setuptools<81